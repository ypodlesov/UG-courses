#include <bits/stdc++.h>
using namespace std;

int main() {

    int n;
    cin >> n;
    vector<bool> used(n+1);
    vector<string> s(n+1);
    for (int i = 1; i <= n; ++i) cin >> s[i];



    return 0;
}